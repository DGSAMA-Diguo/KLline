<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gupiap.kline">
    <!-- SDK 版本声明：必须在 <uses-sdk> 标签中声明，不能写在 <manifest> 属性上，否则系统解析为 0。 -->
    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="35" />
    <!-- 仅申请实时行情和应用更新所需的权限。 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 安卓 8.0 及以上安装应用内下载的更新包时需要此权限。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <application

        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 仅向系统安装器提供应用私有目录中的已校验更新包。 -->
        <provider
            android:name=".UpdateProvider"
            android:authorities="com.gupiap.kline.updates"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
